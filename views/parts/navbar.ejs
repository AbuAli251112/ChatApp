<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <% if(isUser) { %>
                    <li class="nav-item <%= pageTitle === 'Home' ? 'active' : '' %>">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item <%= pageTitle === 'Groups' ? 'active' : '' %>">
                        <a class="nav-link" href="/groups">Groups</a>
                    </li>
                    <li class="nav-item <%= pageTitle === 'Friends' ? 'active' : '' %>">
                        <a class="nav-link" href="/friends">Friends</a>
                    </li>
                    <li class="nav-item <%= pageTitle === 'Search' ? 'active' : '' %>">
                        <a class="nav-link" href="/search">Find People</a>
                    </li>
                    <% } else { %>
                    <li class="nav-item <%= pageTitle === 'Login' ? 'active' : '' %>">
                        <a class="nav-link" href="/user/login">Login</a>
                    </li>
                    <li class="nav-item <%= pageTitle === 'Signup' ? 'active' : '' %>">
                        <a class="nav-link" href="/user/signup">Signup</a>
                    </li>
                <% } %>
            </ul>
            <ul class="navbar-nav ms-auto">
                <% if(isUser) { %>
                    <input id="userId" type="hidden" value="<%= myId %>">
                    <div class="dropdown">
                        <button class="btn btn-primary dropdown-toggle me-2" type="button" id="friendRequestsDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            Friend Requests
                        </button>
                        <ul id="friendRequests" class="dropdown-menu" aria-labelledby="friendRequestsDropdown">
                            <% if (friendRequests.length === 0) { %>
                                <li class="noRequests p-2">no friend requests</li>
                            <% } %>
                            <% for (let request of friendRequests) { %>
                                <li>
                                    <a class="dropdown-item" href="/profile/<%= request.id %>"><%= request.name %></a>
                                </li>
                            <% } %>
                        </ul>
                    </div>
                    <form action="/user/logout" method="POST">
                        <input type="submit" value="Logout" class="btn btn-danger">
                    </form>
                <% } %>
            </ul>
        </div>
    </div>
</nav>